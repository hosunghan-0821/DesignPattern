package java_interface;

public interface Observer{
    //Subject(주체) -> Observer 데이터 Push 방식

    //public void update (float temp, float humidity, float pressure);

    //Observer  ->Subject(주체) 데이터 Push 방식
    public void update();
}
